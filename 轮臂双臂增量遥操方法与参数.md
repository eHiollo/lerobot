# 轮臂双臂增量遥操：方法与参数

本文只讲双臂怎么从人手位姿变成 14 维关节指令，以及沿途每一个会改手感的参数。  
按键、底盘、腰部开关不在范围内。启动与按键见 [轮臂增量遥操作说明.md](./轮臂增量遥操作说明.md)。

适用入口：`use_cpp_incremental_ik:=true`，轮臂走 `wheel_ik_ros_uni_cpp_node`，纯双臂走 `wheel_arm_only_ik_ros_uni_cpp_node`。  
推荐组合：**位置增量 + 姿态绝对**（`use_incremental_hand_orientation:=false`）。

数值以 **v62 / `kuavo_s62`** 为准；v61/v63 滤波截止频率相同，电机 PD 略有差异。

---

## 1. 方法本质

这套遥操不是把人手绝对坐标直接映射到机器人末端，而是 **锚点增量**：

```
进入该侧跟踪瞬间：
  humanAnchor  = 当前 VR 手位置（人手系）
  robotAnchor  = 当前机器人末端 FK（base_link）

跟踪过程中：
  δ_human = (p_vr - humanAnchor) ⊙ delta_scale
  δ_filt  = LPF₂(δ_human)                 # 默认 12 Hz 二阶低通
  p_robot = robotAnchor + δ_filt

  姿态默认绝对：q_robot = slerp(q_robot_anchor, q_vr, t)
  可选全增量：  q_robot ← qΔ * q_robot
```

左右臂完全独立：各有一套锚点、滤波器、肘部参考、7 个关节。一侧松开，另一侧继续跟。

松开该侧：`robotAnchor` 更新为当前 FK，δ 清零，手臂停在原地。  
再按下：`humanAnchor = 当前人手`，从当前机器人位置继续加增量（“推鼠标”）。

---

## 2. 数据流（只留双臂）

```
Quest 手掌 p, q
    → 左右手系转换（monitor_quest3.py）
    → 三点跳变门（仅位置，仅该侧激活时）
    → p 用人手原始坐标做增量
    → q 用标定后的机器人姿态（绝对 slerp / 可选增量累积）
    → 12 Hz / 20 Hz 二阶低通
    → 肘启发式
    → Drake 胸-肘-手点优化
    → Drake 点跟踪 IK（14 臂）
    → Ruckig + α 渐变 + dq 低通
    → /kuavo_arm_traj
    → 控制器 Kalman + fhan 插补
    → VR 专用 PD + vrArmAccelTask
```

循环约 **100 Hz**。左右各算一次增量，再拼成一次 IK。

核心代码：

| 环节 | 文件 |
|---|---|
| UDP / 坐标系 | `noitom_hi5_hand_udp_python/scripts/monitor_quest3.py` |
| 人手位姿预处理 | `motion_capture_ik/src/Quest3ArmInfoTransformer.cpp` |
| 增量 + LPF | `motion_capture_ik/src/WheelIncrementalControlModule.cpp` |
| 跳变门 / 肘 / 调 IK | `motion_capture_ik/src/WheelQuest3IkIncrementalROS.cpp` |
| 参数加载 / 关节顺化 | `motion_capture_ik/src/WheelQuest3IkIncrementalTools.cpp` |
| 一阶段 IK | `motion_capture_ik/src/WheelOneStageIKEndEffector.cpp` |
| 控制器插补 / VR 任务 | `humanoid_controllers/src/humanoidController_wheel_wbc.cpp` |

---

## 3. 输入：双臂实际吃什么

`monitor_quest3.py` 把 Quest 左手系转成 ROS 右手系后，把手掌放进 `/leju_quest_bone_poses`：

```
p' = (-z, -x, y)
q' = (-qz, -qx, qy, qw)
```

`PoseInfo` 里的位置 **不再乘 3 倍**。`scale_factor=3` 只给 TF 可视化。

`Quest3ArmInfoTransformer` 会先用胸/肩/肘/手算出一套机器人空间的手、肘（臂长缩放、过中线修正），然后 **把手位置覆盖回 Quest 原始手掌坐标**，只留下变换后的手四元数：

```cpp
leftHandPose_.position = input.poses[POSE_INDEX_LEFT_HAND].position;  // 原始人手坐标
```

因此增量模块拿到的是：

| 量 | 实际坐标系 | 用途 |
|---|---|---|
| `p_vr` | Quest→ROS 后的人手掌位置 | 算位置增量 δ |
| `q_vr` | 已转到机器人习惯的手姿态 | 绝对 / 增量姿态 |
| `p_robot_anchor` | `zarm_l6/r6` 或 EE 的 FK | 机器人侧锚点 |

位置增量在 **人手空间** 计算，乘 `delta_scale` 后加到 **机器人 FK 锚点**。  
不是“人体手臂按比例缩放到机器人工作空间后再跟随”。

人体臂长缩放（`upper_arm_length` 等）只服务可视化 / 肘几何，**不进位置增量**。迁增量方法时可以整段丢掉；只有改成绝对位置映射才需要它。

---

## 4. 姿态预处理（进增量的那个 q）

人手四元数进增量前，固定做一次设备→机器人旋转（硬编码，不在 launch 里）：

```
biasAngle = 15°
右手: q = q_vr * RotZ(0)     * RotX(-90°) * RotX(+15°)
左手: q = q_vr * RotZ(-180°) * RotX(-90°) * RotX(-15°)
```

再去掉人体胸的 yaw，只留相对胸的手朝向：

```
q ← yawOnlyQuat⁻¹ * q
```

迁到别的头显 / 手套时，这一段几乎一定要重标定。

Transformer 里另有一套人体→机器人几何（**不进位置增量**）：

| 参数 | v62 `kuavo.json` | 作用 |
|---|---|---|
| `upper_arm_length` | 0.2844 m | 机器人上臂 |
| `lower_arm_length` | 0.45 m | 机器人下臂 |
| `shoulder_width` | 0.2527 m | 肩宽 |
| `base_height_offset` | 1.1658 m | 胸→base 高度 |
| `base_chest_offset_x` | 0.1157 m | 胸→base 前向 |
| 测量样本数 | 30 帧/侧 | 够了就用均值缩放 |
| 肩前移上限 | 0.08 m | 肘往前时肩跟着前 |
| 肩内收上限 | 0.15 m | 肘往前时肩内收 |
| 过中线额外内收 | 0.08 m | 手跨中线 |
| 中线拉近 | x>0.15 且 \|y\|<0.2 时最多 5 cm | 手更容易到胸口 |
| 手 x 下限 | 0.1 m | 防手穿到身后 |

---

## 5. 位置增量

进入该侧跟踪时记锚点，之后每帧：

```
rawΔ_x = (p_vr.x - humanAnchor.x) * delta_scale_x
rawΔ_y = (p_vr.y - humanAnchor.y) * delta_scale_y
rawΔ_z = (p_vr.z - humanAnchor.z) * delta_scale_z

Δ = SecondOrderLPF(rawΔ, fc=pos_cutoff_hz, fs=publishRate)
p_des = robotAnchor + Δ
```

LPF 是双线性二阶（Butterworth 型，Q = 1/√2）：

```
fc = min(pos_cutoff_hz, 0.95 * fs/2)
k  = tan(π * fc / fs)
norm = 1 / (1 + √2 k + k²)
b0 = k² * norm
b1 = 2 b0
b2 = b0
a1 = 2 (k² - 1) * norm
a2 = (1 - √2 k + k²) * norm
```

`fc <= 0` 则旁路。进入 / 退出该侧时重置滤波器状态，避免阶跃。

速度由差分估计（主路径不做硬限幅）：

```
dΔ/dt = (Δ - Δ_prev) / dt
dt = 1 / publishRate
```

| 参数 | 配置位置 | 轮臂当前值 | 含义 |
|---|---|---|---|
| `delta_scale_x/y/z` | `launch_quest3_ik.launch` | **1.26** | 人手 1 cm → 臂约 1.26 cm |
| `pos_cutoff_hz` | `kuavo.json` | **12 Hz** | 增量毛刺 / 延迟的主旋钮 |
| `publishRate` | 节点构造 | **100 Hz** | LPF 采样率，须与循环一致 |
| `pos_vel_limit` | launch | 30 m/s | 读了，位置主路径未用 |
| `arm_move_threshold` | launch | **0.002 m** | 相邻帧 \|Δp\| > 2 mm 才算“开始动”；只影响关节 α 渐变，**不挡住增量** |
| `delta_scale_roll/pitch/yaw` | launch | 1.0 | 读进 `deltaScaleRPY_`，轮臂增量主路径未乘 |

---

## 6. 姿态

### 6.1 默认：位置增量 + 姿态绝对

```
q_des = q_robot_anchor.slerp(t, q_vr)
t = LPF₂(1.0, fc=orientation_cutoff_hz)   # 稳态为 1，几乎跟满
```

人手怎么转，机器人末端就转向同一个（已标定）朝向，不累积。

| 参数 | 值 |
|---|---|
| `use_incremental_hand_orientation` | **false**（推荐） |
| `orientation_cutoff_hz` | **20 Hz**（`kuavo.json`） |

### 6.2 可选：姿态也增量

```
qΔ = q_vr_now * q_human_anchor⁻¹
if angle(qΔ) < 0.01 rad:
    qΔ = I
else:
    q_human_anchor = q_vr_now
    q_target = qΔ * q_target

# 再在当前 EE 坐标系裁剪
q_rel = q_ee⁻¹ * q_target
q_rel = limitEulerZYX(q_rel, zyx_limits_final)
q_des = q_ee * q_rel
```

| 参数 | 默认 | 用途 |
|---|---|---|
| `pythonOrientationThresholdRad` | 0.01 rad ≈ 0.57° | 小于此角的姿态增量当 0 |
| `zyx_limits_final` | `[1.4923, 1.4923, 1.4923]` ≈ 85.5° | 最终姿态裁剪（会卡腕） |
| `zyx_limits_ee` | `[1.4923]³` | 相对 EE；几乎不卡锚点 |
| `zyx_limits_link4` | `[1.5708, 0.6, 0.6]` | 相对 link4，pitch/roll 约 34° |

`left/right_hand_quat_offset_*` launch 默认全 0，轮臂增量路径未使用。

---

## 7. 跳点门（输入防飞）

仅在该侧激活时，对手位置做三点检测：

```
|p - p1| > 0.50 且 |p - p2| > 0.50 且 |p1 - p2| < 0.10
→ 用上一帧位置，姿态仍用当前
连续 2 帧都判跳 → 当快速真运动，放行
持续 > 0.2 s → 强制放行
前 3 帧无条件通过
```

| 常量 | 值 | 位置 |
|---|---|---|
| `SPIKE_THRESHOLD` | 0.50 m | `WheelQuest3IkIncrementalROS.h` |
| `SPIKE_RECOVERY_COUNT` | 2 | 同上 |
| `SPIKE_TIMEOUT_DURATION` | 0.2 s | 同上 |

源干净的动捕可以去掉。Quest 偶发飞点靠它挡。

---

## 8. 进 IK 之前：参考点

每侧激活时：

1. 手目标 = 第 5、6 节的 `p_des, q_des`
2. 肘目标用启发式，**不是人体肘**：

```
v = p_link6 - p_ee
elbow_ref = p_link6 + v * (l2 / |v|)
```

`l2` 取 `lower_arm_length`（v62 = 0.45 m）。肘被放到腕的反手方向延长线上，给 IK 一个不打架的猜测。

3. 未激活的那一侧：手 / 肘冻在上一帧约束（相对胸的相对位姿），避免单手跟时另一只手漂。
4. 这些点先过 Drake 胸-肘-手点优化，再进一阶段点跟踪 IK。

launch 里的球 / 盒 / 圆柱约束会被读入，但轮臂路径 **没有做手位置 clip**。真正裁工作空间的是 Drake bounds。

### 8.1 Drake 点优化权重（v62 `drake_chest_elbow_hand_weights`）

| 项 | 值 | 含义 |
|---|---|---|
| `q0_xyz` | [35, 0, 35] | 胸 xyz 位置代价（y=0，不拉左右） |
| `wq0_xyz` | [35, 35, 35] | 胸姿态 |
| `q1` | 5 | 肘 |
| `q2` | 20 | 手（手比肘重） |
| `qv*` | ~1e-3 | 速度平滑 |
| `qa*` / `qjerk*` | 1e-7 / 1e-15 | 加速度 / jerk，几乎放开 |
| `acc_fixed_dt_sec` | 0.01 | 固定 10 ms |

### 8.2 Drake 点优化边界（v62 `drake_chest_elbow_hand_bounds`，真在用）

| 项 | 值 |
|---|---|
| 胸盒 | x∈[0.12, 0.65], y∈[-0.001, 0.001], z∈[0.45, 1.15] |
| 左手盒 | [-1.25, 1.25] × 几乎不限 y × z∈[-0.05, 1.5] |
| 右手盒 | 同上 |
| chest yaw | ±π/4（±45°） |
| chest pitch | [0, 1.2] rad |
| `min_p2_xy_norm` | 0.25（手水平距离加权） |
| `p2_xy_norm_y_weight` | 0.81 |
| `min_p1_xy_norm` | 0.22（肘） |
| `p1_xy_norm_y_weight` | 0.85 |

只迁双臂、不管胸时，点优化仍会改手目标，尤其是水平距离和高度盒。

### 8.3 launch 约束（已加载，轮臂未 clip）

| 参数 | launch 默认 |
|---|---|
| `sphere_radius_limit` | 0.6 m |
| `min_reachable_distance` | 0.08 m |
| `box_min_bound` | `[0.00, -0.6, -0.19]` |
| `box_max_bound` | `[0.4, 0.6, 0.4]` |
| `chest_offset_y_ax` | 0.05 m |
| `left_center` / `right_center` | `[0, ±0.02, 0]` |
| `elbow_min_distance` / `elbow_max_distance` | 0.16 / 0.24 m |
| `default_left_hand_pos_on_exit` | `[0.00, 0.293, -0.16]` |
| `default_right_hand_pos_on_exit` | `[0.00, -0.293, -0.16]` |
| `hand_changing_mode_threshold` | 1.20 m |

---

## 9. IK（14 臂）

`WheelOneStageIKEndEffector::solveIK`，Drake SNOPT，跟踪 EE / link6 / 虚拇指 / 肘 / 肩 / 胸。

v62 `point_track_ik_solver_config`：

| 参数 | 值 | 变大时 |
|---|---|---|
| `eeTrackingWeight` | 5000 | 末端更贴 |
| `link6TrackingWeight` | 5000 | 腕更贴 |
| `virtualThumbTrackingWeight` | 5000 | 姿态更稳 |
| `elbowTrackingWeight` | 1000 | 肘更听话，手可能让步 |
| `shoulderTrackingWeight` | 5000 | 肩更贴 |
| `chestTrackingWeight` | 5000 | 胸（纯双臂可忽略） |
| `jointSmoothWeight0/1` | 1000 | 肩更钝 |
| `jointSmoothWeight2` | 500 | |
| `jointSmoothWeight3` | 300 | 肘 |
| `jointSmoothWeight4/5/6` | 10 | 腕更钝 |
| `jointSmoothWeightDefault` | 50 | 兜底 |
| `accSmoothWeightDefault` | 50 | 关节加速度平滑 |
| `jerkSmoothWeightDefault` | 1 | |
| `refSecondOrderLpfCutoffHz` | **12** | IK 内部参考再滤一次 |
| `refSecondOrderLpfDt` | 0.01 | |
| `dynamicsDt` | 0.01 | |
| `historyBufferSize` | 16 | 用历史解做平滑 |
| `maxIterations` | 4000 | |
| `solverTolerance` | 1e-5 | |
| `constraintTolerance` | 1e-7 | |
| `isWeldBaseLink` | true | 基座焊死 |
| `useJointLimits` | true | URDF 限位 |
| `controlArmIndex` | 2 | 双臂 |

IK 失败则丢掉本帧，沿用上一解。成功则拆成：前 4 下肢 + 后 14 手臂。  
`use_arm_only_ik:=true` 时模型只有 14 DOF，不发胸 / 底盘，增量公式相同。

---

## 10. 关节顺化（IK 之后）

### 10.1 Ruckig 运动学限幅

| 参数 | launch 轮臂值 |
|---|---|
| `max_joint_velocity` | **30 rad/s** |
| `joint_space_acc_limit` | **500** |
| `joint_space_jerk_limit` | **3000** |
| `task_space_acc_limit` | 500 | 读了，主用关节限 |
| `task_space_jerk_limit` | 3000 | 同上 |
| `joint_state_publish_rate_hz` | 100（`kuavo.json`） | 臂发布频率 |

### 10.2 α 渐变（防刚按下猛冲）

该侧开始移动后：

```
α = 0.01 + 0.99 * sin(clamp(t / 2.0, 0, 1) * π/2)
q_pub = (1 - α) * q_prev + α * q_ruckig
```

| 参数 | 值 | 作用 |
|---|---|---|
| `GRIP_TIMEOUT_DURATION` | **2.0 s** | α 从 0.01 拉到 1 |
| `arm_move_threshold` | 0.002 m | 没动就不开始计时 |

2 s 到了还会切上肢 quickMode，控制器跟得更紧。没有这层，刚接触就会冲。

### 10.3 关节速度低通

```
dq_lp = α * dq_lp + (1 - α) * dq
```

v62：`lowpass_dq_filter.alpha = 0.1`（历史权重 0.1，新值 0.9，偏跟手）。

传感器回读给 FK 锚点：`q = 0.99 q + 0.01 q_sensor`，防噪声改锚点。

### 10.4 控制器插补（约 500 Hz）

IK 启动后会调用 `/enable_arm_traj_interpolator`。`task.info` 里 `armTrajInterpKinematicLimit.enable` 默认 false，由服务改成 true。

| 参数 | `kuavo_s62/task.info` |
|---|---|
| `reference_update_period` | 0.010 s |
| `kalman_r_q` | 3e-7 |
| `kalman_r_dq` | 5e-4 |
| `kalman_p0_pos` / `kalman_p0_vel` | 1e-2 / 0.5 |
| `kalman_v_limit` | **30 rad/s**（≤0 会整段禁用） |
| `target_v_alpha` | 0.80 |
| `fast_update_r_scale` | 1.0 |
| `immediate_update_on_new_target` | true |
| `fhan_r` | 5000 |
| `fhan_h0_ratio` | 3.0 → `h0 = 3 ×` 控制周期 |

把 100 Hz IK 补成控制周期轨迹。迁到别的控制器，等价物是“下游轨迹插补 + 速度限”。

---

## 11. 控制器：VR 专用增益

IK 起来后自动打开：

- `/enable_vr_arm_kpkd`
- `/enable_vr_arm_accel_task`
- `/enable_arm_traj_interpolator`

退出节点时关回默认。

### 11.1 电机 PD（v62 `kuavo.json`，`use_vr_arm_kpkd`）

```
vr_ruiwo_kp: 150, 200, 200, 200, 55, 55, 55   # 左右对称，肩→腕
vr_ruiwo_kd:  20,  10,  30,  10,  5,  5,  5
```

肩比日常 `ruiwo_kp` 软（270→150），腕更软（120→55）。日常增益不用于 VR。

下肢 `vr_joint_kp/kd` 与日常相同：`[300, 300, 260, 160]` / `[2150, 2300, 2150, 2150]`。

### 11.2 WBC 加速度任务（`vrArmAccelTask`，`kuavo_s62/task.info`）

```
kp: 400, 400, 400, 400, 100, 100, 100   # 近端硬、腕软
kd:  36,  36,  36,  36,  18,  18,  18
```

对比日常 `armAccelTask`（近端 kp=300，部分关节 64），VR 套更均匀、更跟手。`useVrArmAccelTask` 文件默认 false，由服务打开。

---

## 12. 改手感：参数优先级

最先动这 6 个：

| 参数 | 当前 | 变大 |
|---|---|---|
| `delta_scale_x/y/z` | 1.26 | 同样人手动作，臂走得更远 |
| `pos_cutoff_hz` | 12 | 更跟手、更多毛刺 |
| `orientation_cutoff_hz` | 20 | 姿态更跟、更抖 |
| `arm_move_threshold` | 0.002 | 更晚开始加速（α 更久很小） |
| `GRIP_TIMEOUT_DURATION` | 2.0 s | 起手更肉 |
| `vr_ruiwo_kp` 腕部 | 55 | 腕更硬、更抖 |

IK 跟手 / 顺滑：

| 参数 | 当前 | 变大 |
|---|---|---|
| `eeTrackingWeight` | 5000 | 末端更贴 |
| `elbowTrackingWeight` | 1000 | 肘更听话、手可能让步 |
| `jointSmoothWeight0/1` | 1000 | 肩更钝 |
| `jointSmoothWeight4/5/6` | 10 | 腕更钝 |
| `refSecondOrderLpfCutoffHz` | 12 | IK 参考更尖 |
| Ruckig vel / acc / jerk | 30 / 500 / 3000 | 关节跟得更快 |

---

## 13. 方法迁移

### 13.1 必须带走

1. 人手空间 δ × `delta_scale` + 机器人 FK 锚点
2. 松手清 δ、再按时重锚（推鼠标）
3. 左右独立
4. 姿态绝对 slerp（先不要上姿态增量）
5. 增量上的二阶低通（12 Hz 位置 / 20 Hz 姿态）
6. 刚接触的 α 渐变，或等价的速度爬升

### 13.2 建议一起带走

- 跳点门（源脏才要）
- 肘启发式 + 手权重 >> 肘权重
- 腕关节平滑权远小于肩
- 下游关节限速 / 插补
- 更软的腕 PD

### 13.3 可以不迁

- 人体臂长缩放（位置增量不用它）
- launch 球 / 盒约束（轮臂没 clip）
- 胸 / 底盘 / 按键
- `delta_scale_rpy`、`pos_vel_limit`、quat offset（轮臂主路径无效或未用）

### 13.4 必须重标定的硬编码

- `vrQuat2RobotQuat` 的 15° / ±180° / -90°
- Quest→ROS 的 `(-z, -x, y)`

### 13.5 最小核复现顺序

1. 输入左右手 `p, q`（先完成设备系→机器人系标定）
2. 复现「人手 δ × 1.26 + FK 锚点 + 12 Hz LPF + 姿态绝对 slerp」
3. 挂自己的 IK
4. 再对点优化权重和 VR PD

最小核接口可以是：输入左右手位姿 + 该侧是否激活 + 当前 14 维 FK；输出 14 维关节目标。

---

## 14. 配置文件索引

| 文件 | 双臂相关内容 |
|---|---|
| `src/manipulation_nodes/noitom_hi5_hand_udp_python/launch/launch_quest3_ik.launch` | `delta_scale_*`、Ruckig、姿态限幅、工作空间（未 clip） |
| `src/kuavo_assets/config/kuavo_v62/kuavo.json` | LPF 截止、IK 权重、Drake bounds、VR PD、臂长 |
| `src/humanoid-wheel-control/humanoid_wheel_interface/config/kuavo_s62/task.info` | 手臂插补、`vrArmAccelTask` |
| `WheelQuest3IkIncrementalROS.h` | 跳点门、grip α 时间 |
| `WheelIncrementalControlModule.h` | 增量结构、LPF 默认 10 Hz（会被 JSON 覆盖为 12/20） |
